# Variable del analista de extractos.
#
# VA SOLO EN NETLIFY (Site configuration → Environment variables), NUNCA en
# un archivo .env que se suba, y NUNCA con prefijo VITE_.
# Vite incrusta toda variable VITE_* dentro del bundle público, así que una llave
# con ese prefijo queda visible para cualquiera que abra las devtools.

# Secreto compartido que protege el endpoint del analista. Sin esto, cualquiera
# que encuentre la URL puede mandar PDFs a tu función de Netlify.
# Genera uno largo y aleatorio así:
#     openssl rand -hex 32
# Luego pégalo en Netlify y, la primera vez que abras /finanzas → Analista,
# pégalo también ahí (queda guardado en ese dispositivo).
ANALISTA_TOKEN=


# ---------------------------------------------------------------------------
# Supabase (sincronización de Finanzas entre dispositivos).
#
# Estas DOS SÍ llevan prefijo VITE_, a diferencia de la de arriba, y no es una
# contradicción: la clave "anon" está DISEÑADA para ser pública. Solo identifica
# el proyecto, no tiene permisos propios, y cada tabla que alcanza está protegida
# por Row Level Security dentro de PostgreSQL — la base de datos es la que impide
# que una cuenta lea la de otra, no el navegador.
#
# Lo que NUNCA debe aparecer aquí es la clave "service_role": esa se salta RLS por
# completo y solo va en un entorno de servidor.
#
# Se sacan de: Supabase → Project Settings → API
#
# Si las dejas vacías la app sigue funcionando: se queda en modo local, guardando
# en IndexedDB en el dispositivo y sin pedir login.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
